/**
 * Local development only — copy to `engine/config.js` and drop in a key.
 *
 * config.js is gitignored and is NEVER required in production: deployments get
 * their key from the GEMINI_API_KEY environment variable via /api/generate.
 * If this file is absent the app falls back to that proxy automatically.
 */
export const API_KEY = '';
